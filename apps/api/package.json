{
  "name": "@factory/api",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Factorify — API de entrada de tarefas para o orquestrador",
  "scripts": {
    "dev": "tsx watch -r dotenv/config src/index.ts",
    "build": "tsc --outDir dist",
    "start": "node dist/index.js",
    "test": "tsx tests/api.test.ts"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "@fastify/cors": "^10.0.0",
    "@fastify/helmet": "^13.0.0",
    "@supabase/supabase-js": "^2.0.0",
    "@anthropic-ai/sdk": "^0.39.0",
    "@factory/attnres-memory": "workspace:*",
    "@factory/orchestrator": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
